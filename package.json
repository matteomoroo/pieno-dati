{
  "name": "pieno-dati",
  "version": "2.0.0",
  "description": "Ingest giornaliero degli open data MIMIT sui prezzi carburante. Pipeline tipizzata (schema v2) che genera stations.json, meta.json, history.json, status.json serviti da GitHub Pages.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "build": "node --experimental-strip-types scripts/build.ts",
    "build:legacy": "node scripts/build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
